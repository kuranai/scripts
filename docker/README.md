just run `curl -s https://raw.githubusercontent.com/ktxcx/scripts/master/docker/install_docker.sh | sudo bash`<p>
aftwards add your user to docker group `sudo usermod -aG docker <YourUserName>`<p>
log off and logon
